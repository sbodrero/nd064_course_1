apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
    tag: alpine
  name: nginx-alpine
  namespace: {{ .Values.namespace.name }}
spec:
  ports:
  - port: {{ .Values.namespace.service.port }}
    protocol: TCP
    targetPort: {{ .Values.namespace.service.port }}
  selector:
    app: nginx
    tag: alpine
  type: {{ .Values.namespace.service.type }}
